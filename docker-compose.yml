version: '3.8'

services:
  react:
    build:
      context: .
      dockerfile: front.Dockerfile
    restart: unless-stopped
  nginx:
    build:
      context: .
      dockerfile: nginx.Dockerfile
    ports:
      - "80:80"
    restart: unless-stopped
